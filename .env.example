# ---------------------------------------------------------------------------
# TrustGraph by Verisum — Environment Variables
#
# Copy this file to .env.local and fill in the values:
#   cp .env.example .env.local
# ---------------------------------------------------------------------------

# --- Supabase ---
# Project URL and anon key from: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role key (server-only, never exposed to browser)
SUPABASE_SERVICE_ROLE_KEY=

# --- Site URL ---
# The canonical URL of the deployed site. Used as fallback for magic-link
# redirects and Stripe return URLs. For local dev use http://127.0.0.1:3000.
# In production set to the real domain (e.g. https://trustindex.verisum.org).
# NOTE: This is baked into the JS bundle at build time (NEXT_PUBLIC_ prefix).
# Server-side code prefers request headers; client-side prefers window.location.origin.
NEXT_PUBLIC_SITE_URL=http://127.0.0.1:3000

# --- Stripe ---
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRO_MONTHLY_PRICE_ID=
STRIPE_PRO_YEARLY_PRICE_ID=

# --- Admin codes ---
SYSADMIN_CODE=
VERISUM_ADMIN_CODE=
